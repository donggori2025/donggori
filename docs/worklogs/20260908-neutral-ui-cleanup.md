# 잔여 보라색 UI 정리 — 2026-09-08

## 기준과 범위

- 기준 main: `0579613fd6c60d6c00a5a933b7b49de41583ff4d` (작업 전 fetch 확인).
- 시작 시 Production: `dpl_33XtUVFACSxYod5bvXeBBL79Rvka`, Ready. `www.donggori.com` 연결 및 해당 main의 Vercel 성공 상태와 일치.
- 사용자 승인: 나염 색상을 유지하고 잔여 UI 색상을 검정·회색으로 정리, 의도적인 색상은 판단하여 제외, 검증 후 운영 배포 및 제외 목록 보고.
- 개발 가이드, Cursor 규칙, UI 선별 기록, package/CI, 관련 컴포넌트와 호출자·테스트를 읽고 최신 main에서 작업.

## 변경

1. 마이페이지: 제목 보조 문구, 선택 메뉴, 아바타, 저장 버튼, 이메일 링크, 로딩 아이콘, 이름/연락처 포커스.
2. 추천: 분석 버튼, 진행 막대, 로딩 표시, 선택지 테두리와 링. 실제 화면에서 기존 `border-gray-200`과 선택 테두리 충돌을 발견하여 상태별로 분리.
3. 디자인 의뢰: 여섯 입력 항목의 공통 포커스, 레퍼런스 이미지 선택 영역.
4. 공통 공장 이미지 대체 UI: 분홍 배경과 라벨을 무채색으로 변경. 홈·검색·상세·추천에 공통 적용; 실제 공장 이미지 원본/선택/실패 처리는 유지.
5. 공장 상세: 모든 업종에 공통 적용되던 분홍색 `업종 전문` 배지를 회색/검정으로 변경. 검색·추천의 나염 전용 분류 태그와는 별개임.
6. 가입: 오래된 분홍 로고 참조를 현재 헤더와 같은 `logo_donggori.svg`로 교체. 로고의 남색은 유지.
7. 기존 UI 테스트에 무채색 인터랙션, 나염 색상 보존, 선택 테두리 충돌 방지, 로고/소셜 브랜드색 검사를 추가.

## 제외한 색상과 이유

- 검색·추천의 나염 `#A259FF`와 연보라 배경: 사용자 지정 예외.
- 봉제/샘플/패턴/QC/시야게 등 다른 공정 분류색: 타입별 명시적 데이터 분류 체계이며 잔여 보라색 인터랙션이 아님.
- 동고리 현재 남색 로고, 카카오 노랑/네이버 초록, 오류·성공·대기·진행 상태색: 브랜드 또는 상태 의미 보존.
- AI 추천 문구/버튼의 다색 효과: 의도된 AI 표현. 버튼의 활성화 상태와 선택지는 무채색으로 변경.
- 공통 팝업의 공정 단가표 이미지 속 보라색 행: 파랑/초록과 함께 비용 항목을 구분하는 업로드된 표 이미지. 원본 콘텐츠/DB를 변경하지 않음.
- 실제 사진·기사 이미지·현재 사용되지 않는 차트 팔레트/옛 애니메이션/외부 AI 서비스 아이콘: UI 전역 치환이나 원본 자산 삭제 대상에서 제외. 미사용 분홍 로고 파일은 참조만 교체하고 삭제하지 않음.

## 검증 기록

- Node 22.23.2, npm ci 및 최종 코드 `npm run verify` 성공: 보안 4/4, typecheck, lint 오류 0, 62개 페이지 production build.
- 기능/추천 5/5, UI 8/8 성공. 기존 lint 경고 421개는 유지.
- TS AST 비교: className/공통 inputClass/가입 Image 표현 속성을 제외한 6개 변경 컴포넌트의 로직이 기준 main과 동일.
- API/auth/DB/lib/context/middleware/의존성/lockfile/CI 변경 없음. 환경값 다운로드·변경, DB 쓰기 없음.
- 로컬 빌드는 CI의 공개 테스트 환경값을 프로세스에 주입. 실제 Supabase/OAuth 인증 성공의 증거로 취급하지 않음.
- 로컬 화면 QA는 같은 production build를 사용. 루프백 전용 프록시는 모든 쓰기 차단, 공개 공장 GET만 운영에 전달하며 마이페이지는 명시적 가상 계정/요청 fixture로 표시함. 운영 로그인 완료·프로필 저장·문의 제출 검증과 구분.
- Preview/운영 배포, 최종 화면/API 결과 및 배포 ID/SHA는 이 작업의 PR 후속 댓글에 기록. 이 문서만으로 배포 완료를 주장하지 않음.

## 기존 경고와 복구

- npm audit: 기존 잠금 파일에 9개 항목(중간 2, 높음 6, 치명적 1). 의존성은 변경하지 않음. 치명적 `tar`는 Tailwind 빌드 도구 하위 의존성이고 `xlsx`는 import/export 스크립트에서 사용. 이번 색상 변경으로 전체 보안 문제가 해결됐다고 주장하지 않으며, 업데이트는 별도 범위로 남김.
- 문제 발생 시 이전 정상 Production `dpl_33XtUVFACSxYod5bvXeBBL79Rvka` / main `0579613`가 복구 기준. DB 변경 없음.
- 브랜치 보호/CI 연결/환경설정 변경은 기존 보류 결정을 유지.
