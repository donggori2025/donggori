# 동고리 안정화 구현 로그

## 작업 기준

- 브랜치: `codex/donggori-hardening`
- 기준 커밋: `e7f59be` (`codex/security-hotfix`)
- 목표: 현재 서비스 구조를 단순화하고, 서버 인증·공개 데이터·문의·운영 보안을 실제 배포 가능한 수준으로 정리한다.
- 원칙: 공장은 로그인하지 않으며 관리자가 등록한다. 사용자는 로그인 후 공장 정보와 문의 기능을 사용하고, 최종 상담은 동고리 공식 오픈채팅으로 이어진다.
- 제외: 이 브랜치에서는 Supabase 운영 DB에 SQL을 자동 실행하거나 Vercel 운영 배포를 수행하지 않는다. 필요한 변경은 버전 관리된 마이그레이션과 배포 순서로 남긴다.

## 진행 체크리스트

- [x] 0. 기존 사용자 변경을 보존한 격리 작업 트리와 새 브랜치 생성
- [x] 1. 사용자 인증을 서버 세션 기준으로 통합
- [x] 2. 사용하지 않는 공장 로그인·세션·대시보드 제거
- [x] 3. 공장 공개 데이터를 서버의 제한된 API로 통합
- [x] 4. 문의 저장·조회 권한과 입력 검증 보강
- [x] 5. 매칭 점수·표시 문구·이미지 원천 정리
- [x] 6. 공지·팝업·관리자 API 보강
- [x] 7. SEO·오류 화면·보안 헤더·빌드 검증 보강
- [x] 8. Supabase 보안 마이그레이션 및 적용 순서 작성
- [x] 8-1. 추적 중인 SSH 개인키 파일 제거 및 재추적 방지
- [x] 8-2. 실제 기능과 개인정보 고지 내용의 불일치 제거
- [x] 9. 전체 테스트·빌드·보안 점검과 누락 감사

## 완료 기록

### 0. 브랜치 격리

- 상태: 완료
- 결과: 기존 `/Users/hwanjinchoi/Documents/donggori`의 수정 중인 `.gitignore`, `.DS_Store`를 건드리지 않고 `/private/tmp/donggori-hardening`에 별도 작업 트리를 만들었다.
- 확인: 새 작업 트리가 `codex/donggori-hardening` 브랜치를 가리키며 시작 시 변경 파일이 없었다.

### 0-1. 저장소 비밀정보 긴급 제거

- 상태: 코드 측 조치 완료, 운영 키 폐기·재발급 필요
- 결과: 첫 커밋부터 Git에 포함돼 있던 SSH 개인키 파일을 새 브랜치에서 삭제하고 개인키·인증서·`ghp_` 형식 파일을 다시 추적하지 않도록 `.gitignore`를 보강했다.
- 남은 조치: Git 이력에는 과거 키가 남아 있으므로 소유자가 해당 키를 폐기하고 등록된 공개키도 제거해야 한다. 공유 이력 강제 재작성은 별도 승인과 팀 공지가 필요한 운영 작업이라 자동 수행하지 않았다.

### 1–8. 애플리케이션 하드닝

- 상태: 코드 및 버전 관리 마이그레이션 작성 완료
- 결과: 인증을 서버 세션으로 통일하고, 공장 로그인·가짜 브라우저 권한·샘플 공장 데이터·직접 Storage 업로드·공장 자동 알림을 제거했다. 공개 공장·공지·팝업 응답을 허용 필드로 제한하고, 문의·피드백·관리자 입력 권한과 검증을 보강했다.
- 결과: OAuth state와 내부 경로 검증, 소셜 가입 HttpOnly proof, OTP 해시·실패 제한, 세션 해시·전체 폐기, 비밀번호 10자 정책, 공개 오류 일반화, 보안 헤더·SEO·오류 화면·CI 검증을 반영했다.
- DB: 세션, 소셜 ID 유니크 제약, OTP 시도 횟수, 공지 공개 상태의 배포 전 마이그레이션과 애플리케이션 테이블 잠금의 배포 후 마이그레이션을 분리했다. 운영 DB에는 자동 실행하지 않았다.

### 9. 최종 검증 및 독립 누락 감사

- 상태: 완료
- 결과: 별도 감사에서 발견한 공장 상세 localStorage 인증·문의 저장, 마이페이지 가짜 탈퇴/동의/이미지 UI, 6자 비밀번호 잔존, 저점 추천 채우기, 임의 공장·지도 기본값, 하드코딩 문의 목록, OAuth 환경변수 문서 불일치를 모두 정리했다.
- 검증: 보안 회귀 4/4, 공장 매칭 3/3, TypeScript, production build, `git diff --check`를 통과했다. lint는 오류 0이며 기존 경고는 별도 백로그다. production 의존성 감사는 `0 vulnerabilities`를 확인했다.

## 운영자에게 남은 체크리스트

- [ ] 노출된 SSH 개인키와 연관 자격 증명을 폐기·재발급하고 GitHub/서버 등록을 교체한다.
- [ ] Supabase 백업을 확인한 뒤 배포 전 마이그레이션 4개를 순서대로 적용한다.
- [ ] Vercel Preview/Production 환경변수와 인증 엔드포인트 WAF 규칙을 설정한다.
- [ ] Preview에서 이메일·카카오·네이버 로그인, 문의, 공지·팝업, 관리자 CRUD를 실데이터로 확인한다.
- [ ] Production 배포와 핵심 흐름 확인 뒤 테이블 잠금 마이그레이션을 적용한다.
- [ ] `match-request-files` Storage 공개 여부·보존 기간·기존 파일을 검토한다.
- [ ] 개인정보 처리방침의 처리 항목·보유기간·국외 처리 현황을 실제 운영과 법률 검토로 확정한다.

## 단계별 상세 문서

- 인증·공장 인증 제거: `docs/hardening/auth-and-factory-auth.md`
- 공장·문의·매칭: `docs/hardening/factory-inquiry-matching.md`
- 운영·SEO·빌드: `docs/hardening/platform-and-seo.md`
- DB 및 배포 순서: `docs/hardening/database-and-deployment.md`
- 최종 누락 감사: `docs/hardening/final-audit.md`
